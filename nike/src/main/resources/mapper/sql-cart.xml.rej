diff a/nike/src/main/resources/mapper/sql-cart.xml b/nike/src/main/resources/mapper/sql-cart.xml	(rejected hunks)
@@ -44,5 +44,22 @@
 			and pp.batch = 1
 	order by 	cart_no desc;
 	</select>
+	<insert id="insert" parameterType="Cart" > <!-- 없는 값은 selectKey로 먼저 구함 -->
+		<selectKey keyProperty="cartNo" order="BEFORE" resultType="integer">
+			select 	nvl(max(cart_no),0)+1 cartNo 
+			from 	cart
+		</selectKey>
+	 	insert 	into cart 
+	 	values	(#{cartNo},
+	 			#{email},
+	 			#{productDetailNo},
+	 			1)
+	</insert>
+	<update id="update" parameterType="Cart">
+		update 	cart 
+		set 	cart_quantity=#{cartQuantity}+1
+		where	cart_no=#{cartNo} <!--  -->
+	</update>
+
 
 </mapper>
\ No newline at end of file
