<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ch.nike.mapper.AccountMapper">
	<update id="deleteMember" parameterType="string">
		update member set member_del='Y' where email=#{email}
	</update>
<!-- 	wishList -->
	<select id="selectWish" parameterType="string" resultType="list">
		select * from wish where email=#{email}
	</select>
<!-- 	cartList -->
	<select id="selectCart" parameterType="string" resultType="list">
		select * from cart where email=#{email}
	</select>
	<select id="selectProThum" parameterType="integer" resultType="product">
		select p.*, pp.product_photo from product p, (select * from product_photo where product_no=
			(select product_no from product_detail pd, cart c where pd.product_detail_no=#{product_detail_no}) 
			and batch=1) pp where p.product_no=pp.product_no
		
	</select>
	
<!-- 	orders  테이블 전체 조인 oracle 패키지의 sql-emp.xml 참고-->

	<select id="selectUserOrder" parameterType="string" resultType="list">
		select * from user_order where email=#{email}
	</select>
	<select id="selectOrderDeatail" parameterType="integer" resultType="user_order_detail">
		select * from 
	</select>
	
	
</mapper>