<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ch.nike.mapper.ProductDetailMapper">
	
	<select id="Color" parameterType="integer" resultType="productDetail">
		select distinct color from product_detail where product_no=#{productNo}
	</select>
	<select id="Size" parameterType="integer" resultType="productDetail">
		select distinct product_size from product_detail where product_no=#{productNo}
	</select>

</mapper>