<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ch.nike.mapper.ProductFeatureMapper">
	
	<!-- ProductCon -->
	<select id="featureList" parameterType="integer" resultType="ProductFeature">
		select * from product_feature where product_no=#{productNo}
	</select>
	
	
	
</mapper>