<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ch.nike.mapper.ProductMapper">
	<select id="list" resultType="product"> <!-- 신발 리스트 -->
		select p.*, h.product_photo, h.batch from product p, category c, product_Photo h where p.category_No =c.category_No and 
		p.product_No=h.product_No and large='1'
	</select>
	<select id="productlist" resultType="product">
		select p.*, product_photo, batch from product p, category c where p.category_no=c.category_no and large='신발' and small='러닝'
	</select>
</mapper>