<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ch.nike.mapper.ProductMapper">
	<select id="list" resultType="product"> <!-- 메인페이지 신발 리스트 by창률 -->
		select p.*, h.product_photo from product p, category c, product_Photo h where p.category_No = c.category_No 
   	 	and p.product_No=h.product_No and large='상의' and batch='1'
	</select>
	
	<select id="productlist" parameterType="category" resultType="product"><!-- 상품리스트 by창률 -->
		select p.*, h.product_photo, h.color product_photo from product p, category c, product_Photo h 
  		where p.category_no = c.category_no and p.product_No=h.product_No and large = '상의' and small = '후드' and batch ='1'
	</select>
	
	<select id="count" parameterType="category" resultType="interger">
		select count(*) from product p, category c where p.category_no = c.category_no and c.large = '상의' and c.small = '후드'
	</select>
</mapper>